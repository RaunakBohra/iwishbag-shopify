{
  "name": "iwishbag-shopify",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant e-commerce platform for Nepal",
  "workspaces": [
    "frontend",
    "admin",
    "api",
    "shared",
    "workers/catalog-events"
  ],
  "scripts": {
    "dev": "concurrently \"npm run --workspace admin dev\" \"npm run --workspace api dev\"",
    "build": "npm run --workspace admin build && npm run --workspace api build",
    "deploy": "npm run --workspace admin build && npm run --workspace api deploy",
    "prisma:migrate": "npm run --workspace api prisma:migrate",
    "prisma:generate": "npm run --workspace api prisma:generate",
    "prisma:studio": "npm run --workspace api prisma:studio",
    "lint": "npm run --workspace admin lint && npm run --workspace api lint",
    "type-check": "npm run --workspace admin type-check && npm run --workspace api type-check"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
